public enum Suit {

    HEARTS,
    DIAMONDS,
    SPADES,
    CLUBS

//    always use uppercase for value names in enums
//    Only use enums if you have a pre-defined list of options and it can only be those options.
//    Ordinal like index in arrays but for enum, starts at 0.
}
